<configuration name="acl.conf" description="Network Lists">
  <network-lists>

    <list name="dl-candidates" default="allow">
      <node type="deny" cidr="10.0.0.0/8"/>
      <node type="deny" cidr="172.16.0.0/12"/>
      <node type="deny" cidr="192.168.0.0/16"/>
    </list>

    <list name="rfc1918" default="deny">
      <node type="allow" cidr="10.0.0.0/8"/>
      <node type="allow" cidr="172.16.0.0/12"/>
      <node type="allow" cidr="192.168.0.0/16"/>
    </list>

    <list name="proxies" default="deny">
    </list>

    <list name="sbc-phone" default="deny">
      <!-- The following line will break the configuration on purpose
           to force the configuration by the user. -->
      Enter the IP addresses of your proxy servers below.
      <node type="allow" cidr=""/>
    </list>

  </network-lists>
</configuration>
